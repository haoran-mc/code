#include <cstdio>
const int maxn = 1e5 + 5;
int a[maxn];

int main() {
    int n, m;
    scanf("%d %d", &n, &m);
    for (int i = 0; i < n; ++i)
        scanf("%d", &a[i]);
    while (m--) {
        int k; scanf("%d", &k);
    }
    return 0;
}
