代码示例：调试 (模拟) 计算机硬件

`CFLAGS -ggdb -Wall`

CPU Reset 后，CPU 完全是 “无情的执行指令的机器”。我们构造一个 512 字节的 “Master Boot Record” 作为磁盘镜像装在在模拟器上，并且用 gdb 观察指令在处理器上的执行。

我们所生成的磁盘镜像是满足 IBM PC Specification 的——因此任何支持 x86 “Legacy Boot” 的 Firmware 都应该可以正确加载它。Firmware 的实现很多：从最早的 IBM PC，到之后的 “兼容机”，再到 QEMU 使用的开源 SeaBIOS。
