```cpp
//增加了一些注释
#include <cstdio>
#include <string>
#include <iostream>
#include <algorithm>
using namespace std;

int N;  /*考生数*/
int M;  /*考题数*/
int G;  /*分数线*/
int score[10];
struct info {
    string ID;  //考生ID
    int total;  //此变量未使用，
    int ques;   //过题数
    int sum;    //考生得分
}stu[1000];

/*sort函数的参数*/
bool cmp(info stu1, info stu2) {
    if (stu1.sum == stu2.sum)
        return stu1.ID > stu2.ID;
    else
        return stu1.sum > stu2.sum;
}

int main()
{
    while (scanf("%d", &N) && N != 0) {
        scanf("%d", &M);
        scanf("%d", &G);
        for (int i = 0; i < M; i++)
            scanf("%d", &score[i]);

        int count = 0;  /*及格人数*/
        for (int i = 0; i < N; i++) {
            cin >> stu[i].ID;
            scanf("%d", &stu[i].ques);
            stu[i].sum = 0;
            for (int j = 0, title; j < stu[i].ques; j++) {
                scanf("%d", &title);
                stu[i].sum += score[title - 1];
            }
            if (stu[i].sum >= G)
                count++;
        }
        sort(&stu[0], &stu[N - 1], cmp);
        printf("%d\n", count);
        for (int i = 0; i < count; i++) {
            cout << stu[i].ID << " ";
            printf("%d\n", stu[i].sum);
        }
    }
    return 0;
}
```

1. struct结构体的定义与使用，你的代码太乱了，这种题用结构体写条理清晰，估计你自己写的也乱
2. 头文件尽量使用C++的头文件，.h结尾的头文件是C里面的，c开头的是C++的，虽然里面的东西一样，但是尽量使用C++的头文件，更规范
3. 我的代码里用到了string容器，如果不了解，可以换成char定义的字符数组，因为string比较好用，string容器在头文件\<string>里面，与\<cstring>不同，\<cstirng>与\<string.h>是一样的，\<string>是STL里面的，可以了解，算法笔记里有
4. 如果换成char型字符数组，输入时就不需要用cin了
5. 数组t的本意是简单散列(一种算法)，可以使用bool型定义
6. 既然t数组只是记录当前学生是否通过，即使赋值1，也可以在后面使用if (t[j] == 1)，来判断，不需要t[j] = j;
7. 使用t数组保存已通过学生了，再对数组排序，就找不到了，刻舟求剑，所以不能改变数组中元素位置
8. 排序也不是这样排的，至少双重循环，看第二次讲课中冒泡排序代码
9. 我的代码里只有string，struct结构体，sort函数是新语法，没有涉及到算法知识，重点看排序函数sort，sort在头文件\<algorithm>中，使用方法在算法笔记中有。以后写题凡是遇到排序的，基本都是它了，不会自己写快速排序，冒泡排序函数，自己写的函数速度也比不上sort函数
10. 最后是代码中的变量名，尽量取一些score这种一目了然的变量名，不会浪费多少时间，看起来让人明白，下面是我自己记录的一些变量名，不是一定要用这些变量名，也可以用自己的，总之像t数组这样的要少用。这个本来就是想在第二次上课时贴出来的，但是考虑到与课程内容无关，就没贴
11. 你的代码错误太多，我看挺久还是很乱，你就最近几天把我的代码看透，并且能自己写出来就行了，你的要改的话，挺难改

| 单词       | word                | wd          |
|------------|---------------------|-------------|
| 常量       | 1e-8                | eps         |
| 无穷大     | infinity            | Inf         |
|            |                     |             |
| 模         | mod                 | mod         |
| 零         | zero                | zero        |
| 加         | add                 | add         |
| 减         | substract           | sub         |
| 乘         | multiply            | mul         |
| 除         | divide              | div         |
| 和         | sum                 | sum         |
| 差         | difference          | dif         |
| 积         | product             | prt         |
| 商         | quotient            | quotient    |
| 差别       | difference          | diff        |
| 乘数       | multiplier          | mul         |
| 多项式     | polynomial          | poly        |
|            |                     |             |
| 分子       | numerator           | num         |
| 分母       | denominator         | dnm         |
|            |                     |             |
| 进制       | base                | base        |
| 系数       | coefficient         | coef        |
| 指数       | exponent            | expo        |
| 底数       | base                | base        |
|            |                     |             |
| 答案       | answer              | ans         |
| 结果       | result              | result      |
|            |                     |             |
| 中间的     | middle              | mid         |
| 开始       | begin               | begin       |
| 结束       | end                 | end         |
| 开始       | start               | start       |
| 停止       | stop                | stop        |
| 完成       | finish              | finish      |
| 来自       | from                | from        |
| 朝...方向  | to                  | to          |
| 入         | in                  | in          |
| 出         | out                 | out         |
| 输入       | input               | input       |
| 输出       | output              | output      |
| 向上的     | up                  | up          |
| 向下的     | down                | down        |
| 高的       | high                | high        |
| 低的       | low                 | low         |
| 深的       | deep                | deep        |
| 浅的       | shallow             | shallow     |
| 上界       | upper bound         | upper_bound |
| 下界       | lower bound         | lower_bound |
| 成功       | succeed             | succeed     |
| 失败       | fail                | fail        |
| 第一个     | first               | first       |
| 最后一个   | last                | last        |
| 前一个     | previous            | pre         |
| 下一个     | next                | nxt         |
|            |                     |             |
| 逆元       | inverse element     | inv         |
| 质数       | prime               | prime       |
| 指针       | pointer             | ptr         |
| 动态规划   | Dynamic programming | dp          |
| 哈希表     | hash                | hash        |
| 堆         | heap                | heap        |
| 字符       | char                | ch          |
| 字符串     | string              | str         |
| 在队列中   | in queue            | inq         |
| 流         | flow                | flow        |
| 树         | tree                | tree        |
| 结点       | node                | node        |
| 队列       | queue               | quu         |
| 向量       | vector              | vec         |
| 一对       | pair                | pair        |
| 最小生成树 | mst                 | mst         |
| 栈         | stack               | st          |
| 前缀和     | prefix and          | prefixAnd   |
|            |                     |             |
| 计数       | count               | cnt         |
| 标志       | flag                | flag        |
|            |                     |             |
| 初始化     | initialize          | init        |
| 最初的     | original            | orgn        |
| 临时的     | temporary           | tmp         |
| 后继       | succeed             | succeed     |
| 标号       | index               | idx         |
|            |                     |             |
| 顶点       | vertex              | vertex      |
| 边         | edge                | edge        |
|            |                     |             |
| 值         | value               | val         |
| 联合       | union               | union       |
| 建立       | build               | build       |
| 位置       | position            | pos         |
|            |                     |             |
| 深度       | depth               | depth       |
| 种类       | variety             | var         |
| 需求       | demand              | dem         |
| 度数       | degree              | degree      |
| 删除       | delete              | del         |
| 时间       | time                | tim         |
| 库存       | stock               | stock       |
| 价格       | price               | price       |
| 距离       | distance            | dist        |
| 最好的     | best                | best        |
| 得到       | get                 | get         |
| 颜色       | color               | color       |
| 秩         | rank                | rank        |
| 询问       | query               | query       |
| 尺寸       | size                | size        |
| 比较       | compare             | compare     |
| 当前量     | current             | current     |
| 因子       | factor              | factor      |
| 属于       | belong              | belong      |
| 额外的     | extra               | extra       |
| 插入       | insert              | insert      |
| 祖先       | ancestor            | ancestor    |
| 障碍       | block               | block       |
| 长度       | length              | length      |
| 极限       | limit               | limit       |
| 矩阵       | matrix              | matrix      |
| 剩余       | residual            | residual    |
| 分裂       | split               | split       |
| 访问       | visit               | visit       |
| 判定       | check               | check       |
| 判断       | judge               | judge       |
| 迷宫       | maze                | maze        |
| 坐标       | coord               | coord       |
| 路线       | route               | route       |
| 括号       | bracket             | bracket     |
|            |                     |             |
| 使用过的   | used                | used        |
| 旧的       | older               | older       |
| 修改       | modify              | modify      |
| 新的       | fresh               | fresh       |
| 更新       | update              | update      |
|            |                     |             |

```cpp
#include<cstdio>
#include<cstring>
int main()
{
	int n;  /*考生数*/
    int m;  /*考题数*/
    int g;  /*分数线*/
	char c[100][100];    /*考生ID*/

	while(scanf("%d", &n) && n != 0) {
        scanf("%d%d", &m, &g);
        int t[n] = {0};  /*bool t[n] = {false};//判断是否过界*/
        int M[m] = {0};  /*题目分值*/
        int b[n][m] = {0};
        int shu[n];
        for(int i = 1; i <= m; i++)
            scanf("%d", &M[i]);        /*题目分值*/

        for(int j = 1; j <= n; j++) {  /*考生答题信息*/
            scanf("%s", c[j]);
            scanf("%d", &shu[j]);      /*过题数目*/
            for(int k = 1; k <= shu[j]; k++)
                scanf("%d", &b[j][k]); /*第j个考生b[j][k]题通过*/
        }

        int sum1 = 0;         //总分
        int sum2 = 0;         //及格人数
        int sum3[n] = {0};    //及格人数分数
        for (int j = 1; j <= n; j++) {
            for (int i = 1; i <= shu[j]; i++)
                sum1 += M[b[j][i]];  //计算每位学生的总分

            if (sum1 >= g) {  //判断是否过线
                sum2 += 1;    //计算过线人数
                t[j] = j;     //为过线学生登记一个“编号”  //******bool t[n] = {false};  t[j] = true;
            }
            sum3[j] = sum1;   //登记每一位学生分数
            sum1 = 0;
        }

        printf("%d\n", sum2); //输出过线人数
        int x;
        int z;
        char y[100];
        for(int i = 1; i <= n-1; i++) {
            if(sum3[i] < sum3[i+1]) {
                x = sum3[i];
                sum3[i] = sum3[i+1];
                sum3[i+1] = x; //交换分数
                strcpy(y, c[i]);
                strcpy(c[i], c[i+1]);
                strcpy(c[i+1], y); //交换学号
                z = t[i];
                t[i] = t[i+1];
                t[i+1] = z; //交换“编号”
            }
        } //按照分数排名完成

        for (int i = 1; i <= n-1; i++) {
            if (strcmp(c[i], c[i+1])>0) {
                x = sum3[i];
                sum3[i] = sum3[i+1];
                sum3[i+1] = x;
                strcpy(y, c[i]);
                strcpy(c[i], c[i+1]);
                strcpy(c[i+1], y);
                z = t[i];
                t[i] = t[i+1];
                t[i+1] = z;
            }
        }  //分数一样的按照考号升序排列完成
        for(int i = 1; i <= n; i++)
            if(t[i] != 0)
                printf("%s %d\n", c[i], sum3[i]);
    }
    return 0;
}
```
